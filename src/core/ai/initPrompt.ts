import type { SurveyData } from '@/store/fileStore';

/** Stub — will be replaced in Task 3 with full prompt assembly */
export function assembleInitPrompt(projectName: string, _survey: SurveyData): string {
  return `Initialize architecture for ${projectName}`;
}
