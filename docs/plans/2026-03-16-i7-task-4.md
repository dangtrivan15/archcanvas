# Task 4: @root/ Cleanup Sweep

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:writing-plans to plan
> the implementation steps for this task.

**Scope:** Replace all remaining @root/ references across stores, AI integration, CLI commands, and test files
**Parent feature:** [I7 Task Index](./2026-03-16-i7-packaging-polish-index.md)
**Spec:** `docs/specs/2026-03-16-i7-packaging-and-polish-design.md` §1.4

## Write Set

- Modify: `src/store/graphStore.ts` (~10 lines) — update any `@root/` handling in store actions
- Modify: `src/components/nodes/NodeRenderer.tsx` (~5 lines) — change `getCanvas(node.ref)` to use ref-node `id` consistently
- Modify: `src/core/ai/webSocketProvider.ts` (~20 lines) — update `canvases.get(ref)` lookups to use ref-node `id`; update describe/list output that formats ref nodes
- Modify: `src/core/ai/systemPrompt.ts` (~15 lines) — update cross-scope examples from `@root/` to `@<ref-node-id>/` syntax
- Modify: `src/cli/commands/describe.ts` (~10 lines) — update ref-node output formatting
- Modify: `src/cli/commands/list.ts` (~5 lines) — update `ref:` type display
- Modify: `src/cli/commands/search.ts` (~5 lines) — update ref type display
- Modify: `src/cli/commands/add-edge.ts` (~10 lines) — update help text for cross-scope syntax
- Tests: Update all test files with `@root/` fixtures (~200 lines across ~10 files)

## Read Set (context needed)

- `src/store/graphStore.ts` — current cross-scope handling (120 lines)
- `src/components/nodes/NodeRenderer.tsx` — current `getCanvas(node.ref)` at line 17 (120 lines)
- `src/core/ai/webSocketProvider.ts` — current `canvases.get(ref)` at line 476 (622 lines, focus on lines 440-520)
- `src/core/ai/systemPrompt.ts` — current prompt text (59 lines)
- `src/cli/commands/describe.ts` — current ref output
- `src/cli/commands/list.ts` — current ref type display
- `src/cli/commands/search.ts` — current ref type display
- `src/cli/commands/add-edge.ts` — current help text
- Run `grep -r "@root/" src/ test/` to find ALL remaining references

## Dependencies

- **Blocked by:** Task 1 (storage foundation), Task 2 (engine changes), Task 3 (inherited edges UI)
- **Blocks:** None (terminal task for §1)

## Description

This is a sweep task that replaces all remaining `@root/` references across the codebase after Tasks 1-3 have updated the core layers. The changes per file are small (5-20 lines each) but span many files.

**Approach:**
1. Run `grep -r "@root/" src/ test/` to identify ALL remaining references
2. For each file, replace `@root/` with the new `@<ref-node-id>/` pattern
3. For `NodeRenderer.tsx`: change `getCanvas(node.ref)` to use the ref-node `id` — since the canvases map is now keyed by ref-node `id` (Task 1), and `node.ref` is now a filename with `.yaml`, the lookup must use the node's `id` field, not `ref`
4. For `webSocketProvider.ts`: same pattern — update `canvases.get(ref)` calls where `ref` was previously the same as the node id. Now use `node.id` for map lookup.
5. For `systemPrompt.ts`: update the example text that teaches Claude Code about cross-scope references
6. For CLI commands: update any help text or output formatting that mentions `@root/`
7. For test files: update ALL `@root/` fixtures to use the new syntax

**Key risk**: This task touches many files. Each change is small and mechanical, but missing a reference breaks tests. The grep-first approach ensures completeness.

**Acceptance criteria:**
- `grep -r "@root/" src/ test/` returns zero results
- All unit tests pass
- All E2E tests pass
- CLI help text shows new cross-scope syntax
- System prompt uses new syntax
- NodeRenderer correctly resolves canvas via ref-node `id`
